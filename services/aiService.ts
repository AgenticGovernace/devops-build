/**
 * Provider-neutral public AI generation surface.
 *
 * The implementation remains in the legacy module temporarily so existing
 * consumers can migrate without a breaking change.
 */
export * from './geminiService';
